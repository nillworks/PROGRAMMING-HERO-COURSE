import HeroHeader from './HeroHeader';
import Nav from './Nav';

const Header = () => {
  return (
    <header className="containers">
      <Nav />
      <HeroHeader />
    </header>
  );
};

export default Header;
