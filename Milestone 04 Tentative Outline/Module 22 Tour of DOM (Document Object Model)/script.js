// Js Dom

// fist
const liItemsAll = document.getElementsByTagName('li');
for (let i = 0; i < liItemsAll.length; i++) {
  console.log(liItemsAll[i].innerText);
}

const li = document.getElementById('li');
const lis = document.getElementsByClassName('');
const lisens = document.querySelector('');
const lissen = document.querySelectorAll('');

// innertext

const liser = document.getElementsByClassName('inner');
console.log(liser);
