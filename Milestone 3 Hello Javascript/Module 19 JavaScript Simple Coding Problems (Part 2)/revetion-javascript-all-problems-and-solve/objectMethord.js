// Object Methods : Object.keys() Object.values() Object.entries()

const priceQun = {
  name: 'food',
  price: 200,
};

// 1: সব key
console.log(Object.keys(priceQun));

// 2:সব value
console.log(Object.values(priceQun));

// 3:key+value
console.log(Object.entries(priceQun));
