// 2. Non primitive or referencial -> array and object

let array = [10, 20, 30, 40, 50];
console.log(array.length);

// update array

// last a dokia dai element
array.push(2);

// fast a dokia dai element
array.unshift(4);

// remove the last element
array.pop();

// remove the fast element
array.shift();
